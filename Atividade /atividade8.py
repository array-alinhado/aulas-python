import os
os.system("cls || clear")
import time
vogais = "ABCDE"

"""Escreva um programa que use um laço `for` para imprimir as letras de 'A' a 'E'."""

for letra in vogais:
    print(letra)
    time.sleep(1)